- if @product.errors.any?
  - unless @product.valid?
    - if errors = @product.errors.full_messages_for(column.to_sym)
      %ul.product-error-text
        - errors.each do |error|
          %li= error